# Shopify Custom App (NMI + 3DS) — Пошаговая реализация

## 🎯 Цель
Минимально жизнеспособный плагин (MVP), который принимает платежи через NMI с поддержкой 3DS внутри Shopify Checkout.

---

## 1. Создание каркаса приложения
**Задача:** подготовить основу для разработки.

### Подэтапы:
1. Установить **Shopify CLI** (`npm install -g @shopify/cli`).
2. Сгенерировать проект: `shopify app create node`.
3. Настроить backend (Node.js + Express/NestJS).
4. Настроить frontend (React + Polaris).
5. Подключить тестовый магазин Shopify и проверить локальный запуск.

**Обязательность:** фундамент, без этого невозможно двигаться дальше.

---

## 2. Интеграция с NMI API (базовые платежи)
**Задача:** связать приложение с платёжным шлюзом.

### Подэтапы:
1. Получить sandbox‑ключи NMI (API key, merchant ID).
2. Реализовать метод `authorizePayment` → отправка данных заказа в NMI.
3. Реализовать метод `capturePayment` → подтверждение платежа.
4. Обработать ошибки (decline, insufficient funds).
5. Протестировать базовый платёж без 3DS.

**Обязательность:** критично, именно NMI выполняет платежи.

---

## 3. Подключение 3DS (frictionless flow)
**Задача:** добавить базовую аутентификацию карт.

### Подэтапы:
1. Настроить вызов `init3DS` через NMI API.
2. Обработать успешный frictionless результат.
3. Сохранить статус в базе.
4. Обновить заказ в Shopify.

**Обязательность:** обязательно, иначе платежи не будут соответствовать требованиям банков.

---

## 4. Подключение 3DS Challenge flow
**Задача:** реализовать сценарий подтверждения клиента.

### Подэтапы:
1. Получить ответ NMI: `challenge_required`.
2. Реализовать редирект клиента на ACS (страница банка).
3. Обработать callback после challenge.
4. Завершить authorize/capture.
5. Сохранить результат и liability shift.

**Обязательность:** обязательно для полноценного продакшн‑решения.

---

## 5. Логирование транзакций
**Задача:** обеспечить прозрачность и отладку.

### Подэтапы:
1. Создать таблицу `payments` в PostgreSQL/MySQL.
2. Добавить поля: `payment_id`, `status`, `3ds_result`.
3. Реализовать запись при каждом шаге.
4. Настроить просмотр логов через админку или консоль.

**Обязательность:** желательно (для MVP можно минимизировать, но для продакшн обязательно).

---

## 6. Админка (минимальная панель)
**Задача:** дать удобный интерфейс для управления.

### Подэтапы:
1. Создать страницу «Transactions» в Shopify Admin.
2. Отобразить список платежей.
3. Добавить кнопку «Refund».
4. Реализовать вызов NMI refund API.

**Обязательность:** желательно (для MVP можно отложить, но для эксплуатации обязательно).

---

## 7. Webhooks и синхронизация
**Задача:** поддерживать консистентность данных.

### Подэтапы:
1. Подключить Shopify webhooks (orders, payments, refunds).
2. Реализовать обработку событий.
3. Обновлять статусы в базе.
4. Проверить идемпотентность (повторные события не ломают данные).

**Обязательность:** обязательно, иначе статусы будут расходиться.

---

## 8. Тестирование в sandbox
**Задача:** убедиться, что всё работает корректно.

### Подэтапы:
1. Проверить frictionless flow.
2. Проверить challenge flow.
3. Проверить возвраты.
4. Смоделировать ошибки (timeout, decline).
5. Подтвердить корректность статусов в Shopify.

**Обязательность:** обязательно, без тестов нельзя выпускать решение.

---

## ✅ Итог
- **MVP:** этапы 1–4 + 7 + 8.  
- **Полный продакшн:** все этапы 1–8.  
- Логирование и админка можно добавить позже, но они сильно упрощают эксплуатацию.