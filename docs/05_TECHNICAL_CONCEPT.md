# Техническая Концепция: NMI + 3DS

## 🎯 Цель
Создать Shopify‑плагин (custom app), который позволяет клиентам интернет‑магазина безопасно оплачивать товары банковскими картами через **NMI**, с поддержкой **3‑D Secure (3DS)** внутри стандартного Shopify Checkout.

## 🔧 Архитектура

### Основные компоненты
- **Shopify Checkout**  
  Инициирует платёж и вызывает наш плагин.
- **Custom Shopify App (наш плагин)**  
  - Backend: Node.js (TypeScript, Express/NestJS).  
  - Frontend: React + Polaris (Shopify UI Kit).  
  - Авторизация: OAuth 2.0 для Shopify, JWT для внутренних сервисов.  
  - Логика: обработка заказов, вызов NMI API, управление статусами.
- **NMI Gateway API**  
  Приём платежей, возвраты, отчёты, поддержка 3DS.
- **3DS SDK (NMI)**  
  Реализует аутентификацию клиента (frictionless + challenge flow).
- **Database (PostgreSQL/MySQL)**  
  Логирование транзакций, хранение статусов, аналитика.
- **LLM‑модуль (опционально)**  
  Автоматизация документации, генерация boilerplate‑кода, аналитика транзакций.

## 🔒 Поток данных
1. Клиент оформляет заказ в **Shopify Checkout**.  
2. Checkout вызывает наш **Custom App**.  
3. Приложение отправляет запрос в **NMI API**.  
4. NMI инициирует **3DS аутентификацию**:  
   - Frictionless → транзакция проходит сразу.  
   - Challenge → клиент подтверждает личность (SMS‑код, push и т.д.).  
5. NMI возвращает результат → наш App обновляет статус заказа в Shopify.  
6. Все события логируются в базе данных.  
7. LLM‑модуль анализирует логи и помогает в отладке/оптимизации.

## ⚙️ Технологический стек
- **Backend**: Node.js + TypeScript + Express/NestJS  
- **Frontend**: React + Polaris (Shopify UI Kit)  
- **API**: Shopify GraphQL + NMI REST API  
- **DB**: PostgreSQL/MySQL  
- **Security**: OAuth 2.0, JWT, PCI DSS compliance  
- **LLM**: Copilot/ChatGPT API для генерации кода, тестов, аналитики